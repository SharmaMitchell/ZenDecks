import React from "react";
import UserAuth from "../components/UserAuth/UserAuth";

const Login = () => {
  return (
    <div>
      <UserAuth />
    </div>
  );
};

export default Login;
